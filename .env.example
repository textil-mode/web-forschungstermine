# Kopiere zu .env (wird NICHT versioniert) und fülle die Werte.
APP_BASE=/forschungstermine
# Google-Gemini-Key (kostenloser Free Tier, https://aistudio.google.com/apikey)
# für die KI-Extraktion. Leer = KI aus. Optional: GEMINI_MODEL (Default gemini-2.0-flash).
GEMINI_API_KEY=
